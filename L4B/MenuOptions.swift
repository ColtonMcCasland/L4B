import SwiftUI
import SceneKit

struct MenuOptions: View {
	var body: some View {
		HStack() {
			
			Spacer()
						
			HStack {
				
				VStack {
					

					Button("Line / ") {
						// Handle drawing shapes
					}
					Spacer()
				}

				Spacer()
			}
		}
	}
}
